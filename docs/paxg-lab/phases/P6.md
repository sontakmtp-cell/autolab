# Giai đoạn P6 — Tự Tối ưu và Công nhận LoRA Thắng

## 1. Nhiệm vụ
- Xây dựng thuật toán tìm kiếm Bayesian với Optuna TPE (tối đa 30 lượt thử, 10 lượt đầu khám phá, dừng sớm nếu 12 lượt không cải thiện).
- Tái sử dụng trainer, backtest engine và adapter store của các giai đoạn trước.
- Quy trình thẩm định ứng viên nghiêm ngặt:
  1. Thử nghiệm trên 3 đoạn fold ngoài mẫu (30 ngày mỗi fold).
  2. Lặp lại tối đa 3 cấu hình khác nhau tốt nhất với các seed 42, 123, 2026 và lấy điểm trung vị.
  3. Huấn luyện ứng viên cuối từ base trên toàn bộ dữ liệu trước kiểm chứng.
  4. Khóa kín dữ liệu kiểm chứng (90 ngày cuối); chỉ đánh giá đúng 1 lần cho ứng viên cuối cùng.
  5. Đánh giá tính độc lập trên tối thiểu 20 khối không chồng lấn (mỗi khối dài 24 giờ = 24 nến 1h hoặc 6 nến 4h).
- Tự động thay thế adapter khuyến nghị nếu và chỉ nếu thỏa mãn toàn bộ tiêu chí công nhận thắng.

## 2. Tiêu chí Nghiệm thu
- Nhấn Bắt đầu / Dừng tự tối ưu một nút hoạt động mượt mà.
- Dữ liệu kiểm chứng tuyệt đối không bị rò rỉ vào không gian tìm kiếm hoặc dừng sớm.
- Ứng viên kiểm chứng thất bại không được phép thay thế adapter đang khuyến nghị.
- Mọi lần công nhận thắng đều được sao lưu kèm báo cáo chi tiết.

## 3. Lệnh thực hiện
```text
/goal Đọc bộ tài liệu docs/paxg-lab và thực hiện P6: tự tối ưu bằng Optuna TPE, backtest nhiều đoạn, kiểm chứng kín, công nhận LoRA thắng và chờ dữ liệu mới. Dùng horizon theo khung và tối thiểu 20 khối độc lập dài 24 giờ. Kiểm thử rò rỉ, phục hồi và chạy một đợt thật có giới hạn; chỉ hoàn thành P6.
```

---

## 4. Bằng chứng Nghiệm thu Thực tế

### 4.1. Bằng chứng chạy thật sau review PR #6

Ngày 07/09/2026, `scripts/run_p6_tune.py` chạy hoàn tất từ mã nguồn
`d802a42bff7fd57d7d4294dcbf8c47d84279562a` trên RTX 5060 Ti, qua
`AutonomousTuningProtocol.run_tuning_cycle()` và các bước `execute_step()` dùng chung với worker.
Bằng chứng cũ ngày 06/09 đã được thay thế.

- Snapshot PAXGUSDT 1h: 12.651 nến; kiểm chứng `[10491, 12651)`, horizon 24.
- 2 trial với tham số khác nhau; lấy cả 2 cấu hình vào nhánh chọn tối đa 3 cấu hình.
- Mỗi cấu hình được học lại độc lập trên 3 fold × 3 seed (42/123/2026).
- Điểm median eval của cấu hình được chọn: **0.436400**; số epoch cuối: 1.
- Ứng viên `paxg_1h_r16_1788776855` đã lưu và nạp thử thành công.
- Locked Score v1: **-1.929513**; weighted MAE **22.236242**; coverage 80% **73.32%**.
- **90 khối độc lập**; bootstrap 1.000 lần, CI 95% **[-1.442044, 0.366126]**.
- Quyết định **REJECTED_PRESERVE_CURRENT**, trạng thái **WAITING_DATA**. Sổ kiểm chứng ghi đúng quyết định cuối.

Giới hạn: đây là chạy GPU thật trên dữ liệu lịch sử trong kho riêng, không phải kiểm chứng trực tiếp bằng dữ liệu thị trường mới. Dùng `fast_dev_mode`: 1 epoch/64 mẫu mỗi fold, 128 mẫu cho final fit; toàn bộ cửa sổ đánh giá vẫn chạy thật. Không dùng số liệu này để chứng minh chất lượng mô hình đã cải thiện. Kho thử không có incumbent nên baseline là Base; nhánh incumbent và công nhận thắng/sao lưu được kiểm tra bằng test, không được tuyên bố đã xảy ra trong lượt thật này. Ứng viên bị từ chối trước bước backup winner. Chu kỳ nhiều ngày và các race được mô phỏng trong test; không chạy P7 hoặc soak test.

Bằng chứng: [kết quả chạy](p6_evidence/p6_real_run_evidence.json),
[audit gatekeeper](p6_evidence/p6_gatekeeper_audit.json),
[sổ kiểm chứng](p6_evidence/p6_locked_ledger.json).

CI mã nguồn: [build/test thành công](https://github.com/sontakmtp-cell/autolab/actions/runs/34110803113).

### 4.2. Kiểm thử Tự động Toàn diện (`test_p6_tune.py`)
- **41 bài kiểm thử chuyên biệt:** đạt **41/41 passed (100%)** (`pytest tests/paxg_lab/test_p6_tune.py -v`).
- **PAXG Lab + TimesFM3:** đạt **223/223 passed**, 7 cảnh báo thư viện (`pytest tests/paxg_lab/ src/timesfm3/ -q`), 122,51 giây trên Windows ngày 07/09/2026.

Danh mục các bài kiểm tra chuyên sâu:
1. `test_optuna_search_space_constraints`: Ràng buộc không gian tìm kiếm PLAN 3.6, alpha = 2 * rank, effective batch = 16, horizon cố định.
2. `test_optuna_search_space_4h_horizon`: Khung 4h tự động khóa horizon = 6 (24h).
3. `test_invariant_validation_detects_violations`: Phát hiện và từ chối ngay cấu hình vi phạm bất biến.
4. `test_data_leakage_locked_test_protection`: Chứng minh thay đổi nhãn/dữ liệu trong tập kiểm chứng có 0% ảnh hưởng đến quá trình tìm kiếm và dừng sớm.
5. `test_24h_independent_blocks_definition`: Khối 24h chuẩn xác (24 nến cho 1h, 6 nến cho 4h).
6. `test_block_bootstrap_rejection_on_insufficient_blocks`: Từ chối thẩm định nếu số khối < 20.
7. `test_block_bootstrap_significant_positive_improvement`: Xác thực tính toán khoảng tin cậy 95% CI nằm hoàn toàn về phía dương khi có tiến bộ thực chất.
8. `test_block_bootstrap_insignificant_when_not_better`: Nhận diện chính xác khi mô hình không vượt trội baseline.
9. `test_early_stopping_stagnation_callback`: Dừng sớm sau 12 lượt không cải thiện ít nhất 0.5 điểm sau 10 lượt khởi động.
10. `test_multi_seed_median_calculation`: Đánh giá ổn định đa seed [42, 123, 2026] và lấy điểm trung vị.
11. `test_gatekeeper_all_criteria_pass`: Đạt toàn bộ 7 tiêu chí -> công nhận thắng, cập nhật khuyến nghị và tạo bản sao zip.
12. `test_gatekeeper_rejection_insufficient_score_improvement`: Từ chối khi điểm không tăng đủ 2.0 điểm.
13. `test_gatekeeper_rejection_coverage_out_of_bounds`: Từ chối khi dải 80% nằm ngoài [65%, 95%].
14. `test_gatekeeper_rejection_fold_worse_than_5_pct`: Từ chối khi có đoạn kiểm chứng tệ hơn base quá 5%.
15. `test_auto_tune_stop_and_resume_recovery`: Nút Dừng/Bật bảo toàn hàng đợi và study SQLite.
16. `test_locked_only_metrics_gatekeeper`: Gatekeeper chỉ dùng metric từ tập locked test, không eval folds.
17. `test_current_recommended_bootstrap_baseline`: Bootstrap so sánh candidate vs current recommended (không vs Base).
18. `test_non_overlapping_block_origins`: Các khối 24h độc lập không chồng chéo origin.
19. `test_three_independent_fold_trainings`: 3 fold train hoàn toàn độc lập, adapter riêng biệt.
20. `test_final_candidate_full_pretest_boundary`: Final candidate train trên toàn bộ history trước test set boundary.
21. `test_backup_zip_integrity_verification`: Xác thực tính toàn vẹn bản sao zip adapter.
22. `test_worker_auto_trial_production_dispatch`: Worker dispatch AUTO_TRIAL job đúng production path.
23. `test_forecast_priority_interleaving_between_auto_steps`: FORECAST jobs xen kẽ giữa AUTO steps theo priority.
24. `test_continuous_auto_recovery_and_data_wakeup`: Recovery tự động và chuyển WAITING_DATA khi hết data.
25. `test_statistical_lock_rejection_on_reuse`: Từ chối tái sử dụng locked test set đã consumed.
26. `test_contemporaneous_score_comparison`: So sánh score cùng thời điểm, không cross-temporal.
27. `test_origin_alignment_different_contexts_and_gaps`: Căn chỉnh origin với context_len khác nhau và gaps.
28. `test_cancellation_during_trainer_updates`: Hủy trong quá trình train cập nhật trạng thái đúng.
29. `test_backup_checksum_corruption_rejection`: Từ chối backup zip bị corrupted checksum.
30. `test_fixed_epochs_final_retrain`: Final retrain dùng fixed epochs, không early stopping.
31. `test_statistical_lock_timestamp_overlap_rejection_across_shifted_snapshots`: Từ chối locked range chồng chéo timestamp khi snapshot dịch chuyển.
32. `test_optuna_stagnation_early_stop_across_bounded_auto_steps`: Stagnation early-stop hoạt động chính xác qua nhiều bounded execute_step riêng lẻ.
33. `test_cancellation_during_final_fit_and_locked_verification`: Hủy trong FINAL_FIT và LOCKED_VERIFICATION bảo toàn phase.
34. `test_auto_job_failure_and_timeout_transitions_to_paused_error`: Lỗi/timeout worker chuyển state sang PAUSED_ERROR.
35. `test_fail_closed_incumbent_recommended_adapter`: Fail-closed khi incumbent adapter lỗi, không fallback về Base.
36. `test_locked_verification_ledger_terminal_verdicts`: Ledger ghi nhận terminal verdicts (ACCEPTED/REJECTED/FAILED).
37. `test_cross_cycle_fresh_verification_interval_and_20_block_requirement`: Xác thực >=20 khối 24h độc lập và fresh interval giữa các cycle.
38. `test_per_fold_execution_and_interleaving`: execute_step chạy bounded 1-fold units cho TRIAL và MULTI_SEED.
39. `test_stopped_race_condition_never_resurrects_or_enqueues`: Sticky STOPPED không bị ghi đè bởi background transitions.
40. `test_top3_configs_multi_seed_median_selection_and_repropose`: Top-3 unique configs + median selection + repropose prior.
41. `test_locked_verification_ledger_migration_and_legacy_fallback`: Migration backward-compatible cho ledger cũ không có timestamp columns.

### 4.3. Sửa review 5125813196

- Base, dữ liệu học/dừng sớm và candidate dùng chung ranh giới `SplitPlan` của đợt. Cache Base gắn với toàn bộ SHA-256 snapshot và split.
- `STOPPED` mặc định không thể bị tác vụ nền ghi đè. Kiểm tra trạng thái và gửi job tiếp diễn nằm trong cùng giao dịch SQLite; chỉ Bắt đầu/Tiếp tục chủ động mới mở lại.
- Khởi động lại giữ snapshot đã lưu và fold hiện tại; thiếu tệp hoặc sai hash chuyển `PAUSED_ERROR`. Worker không thay snapshot lỗi bằng snapshot mới.
- Kiểm tra đủ 20 khối hợp lệ sau lọc gap và căn chỉnh origin trước khi ghi sổ tiêu thụ kiểm chứng. Scheduler chờ thêm dữ liệu khi chưa đủ.
- Nhánh khôi phục locked range đọc JSON và giữ cả start/end đã lưu.
- Chạy thật phát hiện thêm lỗi truyền `FrozenTrial` vào `study.tell`; đã đổi sang số trial đã lưu. Metadata được ghi trước khi hoàn thành trial.
- Sampler dùng seed xác định theo số trial để job mới không lặp lại cùng chuỗi đề xuất khởi động.
- 11 trường hợp hồi quy mới trong `tests/paxg_lab/test_p6_review.py`, gồm Optuna/SQLite thật qua 6 fold, race khi timeout/wake-up, phục hồi snapshot và dữ liệu có gap.

P7 chưa thực hiện. Không tự merge PR.
